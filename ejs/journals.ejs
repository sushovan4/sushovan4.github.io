```{=html}
<ul>
<% for (const item of items) { %>
  <li>
  [<%= item.year %>] <b><%= item.title %></b>.
  <i><%= item.journal %></i> <%= item.vol %>
  <% if (item.with) { %><br/>With: <%= item.with %><% } %>
  <% if (item.links) { %><br/>Links: 
    <% for (const [venue, link] of Object.entries(item.links)) { %>
    [<a href="<%- link %>"><%= venue %></a>]&nbsp;
    <% } %>
  <% } %>
  </li>
<% } %>
</ul>
```